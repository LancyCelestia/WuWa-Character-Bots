# ChatBot 交接快照（2026-08-31）

**性质**：本文是当前工作树的完整状态快照，覆盖 `docs/handover-2026-08-29.md` 全部增量（该文档第九/十节为历史明细，与本快照冲突时以本快照为准）。
**快照锚点**：分支 `v0.0.1-alpha.1`，HEAD `bbaec91`，**全部改动未 commit**（207 文件 +11017/-41594，另含未跟踪新文件）。
**验证基线**：全量 `dev.ps1 test` **253 passed**、全量 Ruff 通过、`runtime-layout` 通过（三任务全绿，2026-08-31 实测）。测试文件 47 个。

---

## 〇、紧急事项（接手第一件事，2026-08-31 00:35 状态）

**bot 进程仍在运行且持有旧配置**（8080 LISTENING，PID 47924；知识清单还是 4 文件版、today_history 缓存还是 cwd 相对路径）。

- **会删数据的风险**：`SqliteVectorKnowledgeStore.retrieve()` 每次检索都按进程内文件清单执行 `sync_chunks`，**清单外的知识块被删除**——刚入库的 4274 个 `th-*`（历史上的今天）块会被旧进程的下一次知识检索清掉。截至 00:35 核实块数仍完好（35228 总量）。
- **实证**：旧进程 08-31 00:30 又在源码树重建了 `ChatBot/data/today_history_cache.json`（新配置键未被它加载）。重启后删除该文件即可（内容可再生）。
- **操作**：用户 Ctrl+C 停旧实例 → 按老流程重启；重启后核对 `th-*` 块数仍为 4274，并删源码树缓存文件。
- **恢复路径（若 th-* 块已被删）**：MD 源在 `ChatBot_Runtime/data/today_history_kb/md/`，重跑 §2.8 的 embed 命令约 25 分钟即重建。

---

## 一、系统架构速览（接手者必读）

- 入口 `bot.py`（NoneBot 启动、日志限速、Telegram 轮询韧性包装）→ 插件 `plugins/bot_unified_runtime/`（单一核心包）。
- 消息管线：`IncomingMessage` → 策略门控（`policy/gate.py`）→ 能力路由（`runtime/pipeline.py`）→ capability（`capabilities/*.py`）→ 审查 → 渲染 → 发送（`sender/onebot.py`、`sender/nonebot.py`（Telegram/Mail））。
- LLM：`llm/model_router.py`（多供应商故障转移 + 动态注册表 + 请求 deadline）→ OpenAI-compatible 直连；vision 独立注册表（`sources/vision_describe.py`）。
- 解析：`sources/parsers/`（专用解析器为主、`platforms_generic.py` 承载 YouTube/小红书/X/抖音等专用函数 + OG 兜底）；订阅 V2：`sources/subscription_store_v2.py` + `subscription_scheduler.py` + `subscriptions/social_v2.py`（七平台 adapter）+ `subscriptions/music_v2.py`。
- 外部运行数据在 `ChatBot_Runtime/`（venv、日志、SQLite/FAISS、cookies），**不属于源码工作区，勿扫描**；源码外 `ChatBot_Archive/` 存历史归档。

## 二、本周期（2026-08-29 → 08-31）完成工作全清单

### 2.1 后端韧性（交接第九节）

| 项 | 状态 | 落点 |
|---|---|---|
| 9.1 可配置 transport timeout | ✅ 完成 | `sender/timeout.py`（默认 15s、硬上限 600s、调用方显式更小值优先）；`Config.bot_transport_timeout_seconds`；`SETTABLE_KEYS` 热更；OneBot/Telegram/Mail 统一走 `resolve_transport_timeout`；测试 `tests/test_transport_timeout.py` 8 项 |
| 9.2 全链路 monotonic deadline | ✅ 完成 | 新建 `runtime/deadline.py`（`DeadlineBudget`/`DeadlineExceeded`/`apply_request_deadline`）；`Config.bot_request_budget_seconds`（默认 90s，校验 (0,600]）；chat 入口创建一次预算，门控 vision/工具循环每轮/每次 LLM 调用；`ModelRouter.generate(deadline_monotonic=)` 故障转移窗口取自身配置(45s)与请求预算更早者，per-attempt timeout 钳回窗口（防 ulp 舍入）；`CapabilityResult`/`SendRequest` 透传 `deadline_monotonic`（pipeline 构造点拷贝）；发送层 deadline 已过→`FAILED_FINAL`+`kind="deadline_exceeded"` 不发网络调用；`deadline_exceeded` 入安全 kind 集、不可重试、群/频道静默。测试 `tests/test_deadline_budget.py` 14 项 |
| 9.3 分片发送幂等恢复 | ⬜ 未做 | 队列仅请求级回执；需求细节见 handover-2026-08-29.md §9.3 |
| LLM failover 总时限 | ✅ | `BOT_CHAT_FAILOVER_MAX_SECONDS=45`，`last_attempts` 追加 `failover:deadline` |

### 2.2 X/Twitter GraphQL 订阅（交接 10.3，代码层收口）

`sources/subscriptions/social_v2.py` `TwitterGraphQLClient`：

- 授权要求 `auth_token+ct0` 双 Cookie，缺失返回 `auth_required`，**从不匿名抓取**。
- 增量分页：每轮从**最新页**开始，单轮沿 Bottom cursor 翻页，命中持久化 `last_item_id` 或上限（`twitter_max_pages` 默认 3、`twitter_max_items` 默认 100）即停；上一轮 Bottom cursor 仅存诊断不再作入口（修复持续翻旧页缺陷）。
- `user_id` 从 cursor payload 复用，进程重启/target 重载后不重复查 `UserByScreenName`。
- query ID/bearer：context/env 可注入；缺失时从 x.com JS bundle 动态发现，**进程内 TTL 缓存 1 小时**（`_discovered_credentials` 类属性）。
- 错误分类：401/403→`auth_required`；429 保留 Retry-After→`rate_limited`；超时→可重试 `network_error`；账号 suspended/not found/protected→不可重试 `upstream_changed`；GraphQL errors 不再误报健康。
- 时间线形态：`TweetWithVisibilityResults` 包裹（取 `result.tweet`）、tombstone 跳过、promoted 跳过（entryId 前缀 + `itemContentPromoted` **键存在性**判断，空字典也是 promoted）、会话 module 子项展开、置顶旧推文由数值过滤挡下。
- 媒体：`extended_entities` 优先；视频取最高码率 mp4 variant + 预览图 + 时长 + 宽高；URL 去重保序；敏感访问头不入 `MediaAsset`。
- 作者资料入 item `source_payload`：头像/简介/认证/粉丝/关注/推文数/媒体数/注册时间。
- `_parse_dt` 兼容 Twitter 英文日期（`published_at` 不再恒 None）。
- 测试：`tests/test_twitter_subscription_graphql_v2.py` 共 15 项（含分页入口/上限/四种形态/媒体/作者/TTL 缓存/账号分类）。
- **待真实 Cookie 实测**（需有效 `auth_token+ct0` 账号）。

### 2.3 vision 能力

- **聊天 vision（既有，本周期核实）**：relay 模式 = 图片→识图注册表多模态模型→三行文字描述（角色：人物（出处）/文字：逐字转写/画面：一句话）→注入文本模型上下文；direct 模式 = `/bot model vision mode direct`，图片直进声明了 `vision/multimodal/vlm` 标签的主模型，失败回退 relay 一次。发图消息走独立抽签（`BOT_VISION_REPLY_PROBABILITY`，默认 1.0=white1 发图必看）。mface（QQ 表情包）在提取范围。**用户三模式需求（纯文本/多模态/纯识图）已被此架构完全覆盖，无需新开发**；待真实图片走查。
- **vision 接入订阅推送（新）**：`sources/vision_describe.py` 新增 `describe_subscription_item`（条目 media/preview/cover 图，默认 1 图 200 字，失败空串）；`__init__.py` `_deliver_v2_event` 对**无文本**条目且 `bot_vision_enabled` 时追加「图：…」一行，任何失败静默不阻断推送。测试 `tests/test_subscription_vision.py` 3 项。有文本条目不识别（控 VLM 成本）。

### 2.4 媒体质量（用户硬指标）

- **统一选流器** `sources/downloader.py::select_media_streams`（纯函数，已测试）：视频排序 最高分辨率→**杜比视界>HDR>SDR**（`_dynamic_range_tier`：dynamic_range 字段/DV 编码提示→HDR）→帧率→码率；音频排序 **Hi-Res（无损 flac/alac 等）> 杜比全景声（ec-3/eac3/ac-4）> 码率/采样率**；音视频组合超 `max_bytes` 逐级回退到 <上限 的最高画质组合；全部超限返回 `over_limit` 诚实报错，**不悄悄下载低画质**；filesize 缺失用 `tbr×时长` 估算（无法估算则放行不拒下载）。测试 `tests/test_media_quality.py` 7 项。
- **下载流程**：`_download_once` 先 probe 元数据再按显式 format_id 下载（`video_id+audio_id/旧表达式` 兜底）；无 ffmpeg 降级单文件。
- **上限配置**：`bot_download_max_bytes` 默认 **1GB**（1073741824）、`bot_download_max_height` 默认 0（不限）；`downloader.py` 类默认、`capabilities/download.py`、`__init__.py` getattr 回退三处已同步。
- **小红书无水印**：图片 `imageList[].urlDefault` 即无水印原图（既有）；新增 `_xhs_video_stream`（`platforms_generic.py`）从 `note.video.media.stream` 按 av1→h264→hls 提取 sns-video 无水印视频直链，存 `ParsedContent.detail.media.video.url`；**该直链的发送消费未接**（见 2.7）。
- **封面**：B 站 `pic`、小红书 `urlDefault` 均为原始分辨率 URL，卡片直接用，不单独落盘。
- **发送格式**：下载/解析发送侧已是视频段 `video=[{"file":...}]`/图片段，**非文件格式** ✓（符合 QQ 发送限制要求）。
- 边界：B 站杜比/Hi-Res 能否取到取决于 yt-dlp bilibili extractor 暴露的 formats，**未实测**。

### 2.5 文件收发（全新能力，管理员限定）

- 新增 `capabilities/file_exchange.py`：
  - `run_code_debug`：.py 先 `compile()` 语法检查（报错行号），通过后受限子进程运行——**无 shell、`-I` 隔离模式、15s 超时、TemporaryDirectory 工作目录、最小 env（PYTHONDONTWRITEBYTECODE/PATH/SYSTEMROOT/COMSPEC）**；回报退出码/stdout 末 400 字/stderr 末 600 字。.md/.txt 等文本文件只做接收确认。
  - `export_document`：Markdown 块解析（heading/bullet/code/para/table，`parse_markdown_blocks`）→ md/docx（python-docx）/xlsx（openpyxl）/pptx（python-pptx）/pdf（fpdf2+simhei 中文字体）；**fpdf2 两个坑已修**：`multi_cell` 默认 `new_x=RIGHT` 不回左边距，连续渲染抛"无横向空间"，已封装 `_line` 强制 `set_x(l_margin)`；中文字体缺 U+2022（•）字形按零宽度抛错，项目符号改 ASCII `-`。
  - `/bot 文件 <md|markdown|docx|pptx|xlsx|pdf> <主题>`：LLLM（`_build_chat_llm_provider(config)` 每次新建）按文档系统提示词生成 Markdown → 转换 → `upload_group_file`/`upload_private_file` 上传 → 回报文件名+KB。文档库缺失/转换失败诚实回报不伪造。
- `__init__.py` 接线（NoneBot 匹配器注册块内）：
  - `on_notice`（优先级 8，rule=`_is_admin_file_notice`：GroupUploadNoticeEvent/PrivateFileNoticeEvent + `bot_admin_user_ids` 门控）→ `get_file` API（base64 或本地路径）→ 存 `data/downloads/incoming/` → 调试回报。
  - `/bot 文件` 走 `on_message`（优先级 8，`block=True`，**抢占 /bot command 分发**）。
- 依赖：Runtime venv 已装 **python-docx、python-pptx、openpyxl、fpdf2**（2026-08-31 pip 实装验证）。
- 测试：`tests/test_file_exchange.py` 6 项（语法错误/运行成功/运行失败 traceback/文本文件/块解析/五格式导出/指令解析）。

### 2.6 指令与帮助（前周期，仍有效）

- `/bot model list|set <id|auto>|add|update|priority|remove|reset|think <off|low|medium|high>|search on|off|usage [日期]|vision list|add|update|priority|remove|mode relay|direct`；顶层短形式、大小写归一、中文模块别名（模型/帮助/设置/维基/百科）。
- `/bot runtime set <KEY> <VALUE>`：SETTABLE_KEYS 含 `BOT_MODEL_SCHEDULE`、`BOT_VISION_ENABLED`、`BOT_TRANSPORT_TIMEOUT_SECONDS`（数值转换器）等，mtime 热重载。
- `BOT_REQUEST_BUDGET_SECONDS` **未入 SETTABLE_KEYS**（改 .env 需重启）。
- 帮助页三大板块（管理员专属/大模型相关/子功能），`/bot help [主题]`。

### 2.7 「历史上的今天」本地知识库（爬取→导出→入库全链路，2026-08-30/31）

**数据源（用户确认"大型平台可爬"后执行）**：百度百科公开接口——事件列表 `baike.baidu.com/cms/home/eventsOnHistory/{MM}.json` + 词条摘要卡 `api/openapi/BaikeLemmaCardApi`（均免鉴权；摘要卡给词条概述上限 600 字，非全文正文）。

**工具脚本 `scripts/today_history_kb.py`**（注意：曾被并行会话误删一次，已重建，功能全量保留）：

- 流程：12 次月度请求拿全年事件 → 每条按 `link` 解析词条关键词（失败时标题去尾缀兜底）→ 逐条查摘要卡（同词条跨天复用缓存）。
- 参数：`--months`（分片，可多实例并行）、`--days MMDD|MM-DD`、`--sleep`（默认 0.35s 限速，全程未触发风控）、`--export-md`（只导出不爬）。
- 韧性：重试 2 次、断点续跑（已存在天文件跳过）、残缺 JSON 天文件重抓。
- 产物（`ChatBot_Runtime/data/today_history_kb/`，源码树外）：
  - `events/{MM}.json` ×12：原始月度（含 cover/festival）；
  - `days/{MM-DD}.json` ×365（含 02-29）：每条 `{year,title,desc,link,cover,keyword,abstract,abstract_url}`，摘要失败写 `limitation`；
  - `md/th-{MM}.md` ×12：每月一个知识文件，每条事件两行（`## 1785年8月30日：林则徐出生`+摘要），段落式切块不拦腰截断；公元前年份格式已修（`公元前10年8月1日`）。
- **质量对账**：365/365 天、4749/4749 事件与源数据一致；摘要覆盖 **4722/4749=99.43%**（补抓一轮后剩 27 条为词条名差异如"口袋妖怪"vs"精灵宝可梦"，判定无收益停止）。

**入库（用户指定用本机 Ollama，降低 AI 算力消耗）**：

- `.env` 的 `BOT_KNOWLEDGE_FILES` 追加 12 个 md 绝对路径（**4→16，真实 .env 已改**）。
- 嵌入：`build_vector_knowledge_provider(config)` → `store.embed_pending(files=provider._files)`；嵌入链**本地 Ollama bge-m3（127.0.0.1:11434/v1）优先**，远程 dashscope 仅兜底（本轮未动用）。
- 结果：新增 **4426 块**，全库 **35228 块全部向量化**（约 25 分钟）；检索实测「虎门销烟 林则徐」命中 th-06 完整摘要。

**关键坑（必读）**：`retrieve()`/`embed_pending()` 都先 `sync_chunks(files)`，**以传入清单为准删除清单外所有块**。因此：①运行中进程清单过期=一次检索就删新块（见 §〇）；②任何离线入库必须传**全量清单**（4 个 persona/游戏文件+12 个 th md），不能只传增量。

**重建/续跑命令**（th-* 块丢失或新增 md 后用；须 PYTHONDONTWRITEBYTECODE=1，从源码根运行）：

```python
from dotenv import dotenv_values
from plugins.bot_unified_runtime.config import Config, translate_env_keys
from plugins.bot_unified_runtime.character.vector_knowledge import build_vector_knowledge_provider
values = translate_env_keys({k: v for k, v in dotenv_values('.env').items() if v is not None})
config = Config.model_validate(values)
provider = build_vector_knowledge_provider(config)
done, pending = provider._store.embed_pending(files=list(provider._files))
print(done, pending, provider._store.stats())   # 期望 total=35228, embedded=35228
```

- 块级断点续跑：已有 `vector_json` 的块不重嵌；`signature`（base_url|models）变化会触发全库重嵌，**勿随意改 `BOT_EMBEDDING_LOCAL_MODELS`**。
- 查块数（只读）：`SELECT COUNT(*) FROM knowledge_chunks WHERE source_id LIKE 'th-%'`（`file:...?mode=ro`）。

**配套修复**：`config.py` 新增 `bot_today_history_cache_file`（默认 `data/today_history_cache.json`）并加入 `_resolve_runtime_data_paths` 的 `path_fields` 重定向（`data/` 前缀→`BOT_RUNTIME_DATA_DIR`）；`__init__.py` 与 `capabilities/today_history.py` 两处实例化传配置值；`.env.example` 增键。根治缓存写入源码树的问题。

**后续候选（未要求，仅记录）**：接入问答/推送链路（现只进向量库被动检索）；27 条缺失需别名映射表，收益低不建议自动跑。

### 2.8 统一投影层收口（contracts/media.py，交接 10.4 部分完成）

- 媒体 URL 去重且保持源顺序（无 URL 资产不参与去重）——`normalize_projection`。
- `_optional_datetime` 补 Twitter/微博英文日期 `%a %b %d %H:%M:%S %z %Y`（时区保留）；ISO/Unix 秒/毫秒已有。
- `_engagement_from_legacy_stats` labels 补 `bookmark/bookmarkCount`、`quote/quoteCount/引用`、`play/playCount/播放次数`（首个非空键生效；未知仍 None+limitation）。
- 测试：`tests/test_media_metadata_projection_v2.py` 共 12 项。
- 残留：作者统计与内容互动混入审计；消费端全迁嵌套模型后才能删扁平字段（§2.9 条 3）。

### 2.9 明确未做（唯一待办清单）

1. **解析结果视频直发**：content_parser 视频直链→下载→视频段自动链路未接（现在 `/bot download` 可用；解析卡片给出链接）。XHS 无水印视频直链已提取但发送侧未消费。
2. **9.3 分片幂等恢复**（part 级进度/UNKNOWN 确认协议/部分成功续发），需求见 handover-2026-08-29.md §9.3。
3. **10.4 其余六平台字段深化**（B 站/小红书/YouTube/Telegram/Pixiv/微博在线字段；统一投影层三项收口已完成，见 2.8）。
4. **10.5 音乐动态与榜单**（QQ/酷我/酷狗/Spotify 订阅、真实榜单 source）。
5. **10.6 V2 清理**（ParsedContent 扁平投影删除、旧契约迁移后清理、target metadata 持久化 API）。
6. **P4 Crawl Wiki `/bot wiki`**：按用户要求**暂缓**，不得扫描/导入 `D:\Coding\Crawl Wiki`。
7. 人工验收：**先执行 §〇 重启 bot，重启后核对 `th-*` 块数仍为 4274 并删源码 `data/today_history_cache.json`**；X GraphQL 真实 Cookie 实测、B 站杜比/Hi-Res 实测、QQ 文件链路真机（NapCat get_file 返回形态、群文件上传权限）、vision 推送/聊天真实图片走查、9.1 管理员设小值实测三通道发送 + 重启后 `.env` 与 override 优先级确认。

## 三、配置键速查（新增部分）

| 键 | 默认 | 生效 | 说明 |
|---|---|---|---|
| `BOT_TRANSPORT_TIMEOUT_SECONDS` | 15 | 热更 | 发送单次硬超时，(0,600] |
| `BOT_REQUEST_BUDGET_SECONDS` | 90 | **重启** | 请求级总预算，(0,600]，未入 SETTABLE_KEYS |
| `BOT_DOWNLOAD_MAX_BYTES` | 1073741824 (1GB) | 重启 | 下载上限，选流回退阈值 |
| `BOT_DOWNLOAD_MAX_HEIGHT` | 0（不限） | 重启 | 高度上限，0=按选流器最高画质 |
| `BOT_VISION_ENABLED` / `BOT_VISION_MODE` / `BOT_VISION_REPLY_PROBABILITY` | true / relay / 1.0 | 前两者热更 | vision 三开关 |
| `BOT_TODAY_HISTORY_CACHE_FILE` | data/today_history_cache.json | 重启 | 「历史上的今天」每日缓存；data/ 前缀自动重定向到运行时目录 |
| `BOT_CHAT_FAILOVER_MAX_SECONDS` | 45 | 重启 | LLM 故障转移总时限 |

真实 `.env` 本周期改动一处：`BOT_KNOWLEDGE_FILES` 4→16（追加 `ChatBot_Runtime/data/today_history_kb/md/th-*.md` 12 个知识文件，见 2.7）；其余只改 `.env.example`。

## 四、验证与运维

```powershell
# 三任务（唯一验证入口；绕开 dev.ps1 跑 python 必须 PYTHONDONTWRITEBYTECODE=1）
powershell -NoProfile -ExecutionPolicy Bypass -Command "& '.\scripts\dev.ps1' -Task test"
powershell -NoProfile -ExecutionPolicy Bypass -Command "& '.\scripts\dev.ps1' -Task lint"
powershell -NoProfile -ExecutionPolicy Bypass -Command "& '.\scripts\dev.ps1' -Task runtime-layout"

# 启停（单实例铁律；用户提权实例需用户自己 Ctrl+C）
# 判活：8080 监听 + 日志 Bot connected + Start poll + 993 ESTABLISHED
# 日志：ChatBot_Runtime/logs/nonebot.out.log（轮转 .old）、data/runtime_events.log、data/crash.log
```

## 五、风险与约定

1. **并行会话**：用户并行开多个 AI 会话——lint F821/陌生改动先判归属；本周期 `__init__.py` 曾在编辑间隙被并行改动（行号平移）；`scripts/today_history_kb.py` 曾被并行会话误删、已重建（含 `--export-md`），**勿再清理该文件**；源码 `data/today_history_cache.json` 曾被移出使 runtime-layout 转通过，但**旧 bot 进程 08-31 00:30 又重建了一份**（见 §〇，重启后删）。
2. **知识库 sync 删除语义**：`retrieve()`/`embed_pending()` 按传入文件清单删清单外块（见 2.7 关键坑）；运行中进程清单过期会删新块。
3. **子代理并发限额=1**，须串行；合并后全仓 ruff。
4. **最小文件范围**；不扫 Runtime/Archive；不删活动数据（SQLite/FAISS/cookies/日志/卡片 SVG/venv）。
5. **密钥安全**：`.env` 真实密钥只允许 grep 目标行且脱敏输出；历史会话曾误带出密钥（仅工具输出）；建议轮换 Telegram Token/QQ 授权码（用户未确认）。
6. **改动未 commit**：工作树在 `v0.0.1-alpha.1` 分支未提交（含未跟踪新文件）；commit 需用户明确要求。
7. fpdf2 坑与 `itemContentPromoted` 空字典坑已在 2.2/2.5 记录，改相关代码前必读。
