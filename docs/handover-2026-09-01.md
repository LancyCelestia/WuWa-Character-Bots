# ChatBot 交接快照（2026-09-01）

**性质**：接续 `docs/handover-2026-08-31.md` 的周期快照，覆盖 2026-08-31 续轮（本会话）全部增量；冲突时以本快照为准。
**验证基线**：全量 `dev.ps1 test` **267 passed**（+14 项新增测试）、全量 Ruff 通过、`runtime-layout` 通过（2026-09-01 实测）。测试文件 49 个。
**改动状态**：全部未 commit（延续既有约定）。

---

## 一、本周期（2026-08-31 续轮）完成工作

用户指示：按交接 §2.9 继续推进「解析、补全」后端工作；只做后端，前端（卡片 HTML/CSS/渲染）不动；后端文本输出质量优先。范围 = 待办 1（视频直发）+ 3（六平台字段深化）+ 5（V2 清理）+ 文本质量。

### 1.1 V2 清理（交接 §2.9 待办 5，本周期收口）

- **`ParsedContent` 纯嵌套化**（`contracts/media.py`）：删除 14 个扁平投影字段（platform/item_id/item_kind/title/author_name/summary/cover_url/audio_url/canonical_url/stats/parse_depth/page_type/badge/detail）与 `normalize_projection` model_validator；模型只剩 `identity/content/creator/engagement/media/music/provenance`。
- **新构建器 `build_parsed_content(**flat)`**（模块级）：承接原 normalize 逻辑——stats→engagement、detail.author→creator、detail.media/images/video→media、发布时间→content.published_at（新增 video.pubdate 兜底）；平台特有键收进 `content/creator/engagement.platform_extra`（content 收 page_type/badge/cover_url/images/video 扩展/episodes/live/comments/goods/related 等；engagement 收分辨率/图片数量/music_card 等；`CreatorMetadata` 新增 `platform_extra` 收作者扩展键）。
- **渲染投影 `flat_projection(item)`**（`output/card_render/bridge.py`）：嵌套模型 → 渲染投影字典，`parse_to_render_payload` 入口先投影再走原逻辑；**卡片区块与迁移前等价**（个别标量统一格式化：发布时间补秒、时长转 int 秒）。`templates.card_payload_from_parse` 同步改读投影（**模板 HTML/CSS 零改动**）。
- **消费端全迁移**：`capabilities/content_parser.py`（能力层 + 订阅推送 + `_render_parse_body` 嵌套渲染）、`capabilities/music.py`（`music_audio_url/music_cover_url/_music_card_parts` 读嵌套 + platform_extra）、订阅推送。
- **21 个 platforms_*.py 构造点** `ParsedContent(` → `build_parsed_content(`（机械替换，参数不变）；3 处链式扁平读取（moegirl/pixiv_ranking/bilibili_goods `_og_goods` 重写）改嵌套。
- 测试：投影测试改用 builder（`test_media_metadata_projection_v2.py` 等 5 个文件）；新增「无扁平投影字段」守卫测试（`test_media_contract_v2.py`）。

### 1.2 六平台字段深化（交接 §2.9 待办 3，10.4 主项完成）

统一规则：权威数据进嵌套模型，未知保持 None/不伪造；全部离线 fixture 测试（`tests/test_platform_field_deepening_v2.py` 5 项）。

| 平台 | 本周期深化 | 落点 |
|---|---|---|
| B站 | 分P 列表（index/标题/时长） | `content.platform_extra["episodes"]` → 卡片分P区块（原有摘要行共用） |
| 小红书 | 全图集进 media；sns-video 无水印直链 + 宽高/时长/封面 → video 资产（原 `detail.media.video` 字典形态从不被消费，现改 `detail.video` 正确进 media） | `_xhs_from_initial_state` |
| YouTube | 时长（lengthSeconds）、视频简介（shortDescription，`_yt_unescape` 修复字面中文被 unicode_escape 破坏）；订阅/视频数 → creator（`_AUTHOR_STATS_KEYS` 增「订阅」「视频数」）；`_yt_count` 解析本地化计数（"5.6万位订阅者"） | `_youtube_watch_enrich`/`_youtube_about_enrich` |
| Telegram | 消息时间（`<time datetime>`）、图集图片、视频直链（src/data-src）、频道标题/头像/简介/订阅数；**telegram.me 域名支持**（原注册表有规则但 parser 正则只匹配 t.me）；私有邀请/不支持链接改抛 `ParseFailure`（计划文档要求，原抛 ValueError） | `platforms_telegram.py` |
| Pixiv | 分镜全量 URL/宽高进 media（原只进摘要文本行）；作者结构化统计（fans/following/illusts/manga/novels → creator）；R-18（x_restrict）与动图（分帧 ZIP 需登录）诚实分类 | `platforms_pixiv.py` |
| 微博 | 全图进 media、视频 page_info（直链/时长/宽高/预览）、作者 following/statuses/认证/注册时间/主页 | `platforms_weibo.py` |

### 1.3 解析结果视频直发（交接 §2.9 待办 1，新能力）

- **触发条件**：解析结果 media 含**带直链 URL 的 video 资产**（小红书 sns-video/Telegram 视频消息等）且 `BOT_CONTENT_VIDEO_AUTO_SEND=true`；B站/YouTube 无直链不触发（保持「下载：/bot download」提示，避免重型下载误触发）。
- **链路**：`downloader.download(直链)` → 视频段 `video=[{"file": path}]` 随卡片发送；body 追加「视频已发送 ✓（大小）」；audit 标签 `video_auto_sent`。
- **降级**：下载失败/超限/yt-dlp 缺失一律静默回退「下载：」提示，绝不阻断卡片。
- **配置**：`BOT_CONTENT_VIDEO_AUTO_SEND`（默认 **true**；可热更：`SETTABLE_KEYS` + `_bool_converter`）；大小上限复用 `BOT_DOWNLOAD_MAX_BYTES`。
- 测试：`tests/test_content_video_auto_send.py` 6 项（成功/开关关/失败降级/无直链/yt-dlp 缺失/非视频）。

### 1.4 后端文本输出（待办 D）

- `_render_parse_body` 按嵌套模型渲染：标题/作者/数据/发布/简介/链接分区；互动字段统一中文标签（播放/点赞/评论…）+ platform_extra 标量透传；内部键（时长/发布时间/AV号 等 `_INTERNAL_STAT_KEYS`）不进入公开数据行；发布时间本地时区格式化 `YYYY-MM-DD HH:MM`。

### 1.5 代码审查与修复（engineering-code-reviewer 双轴审查）

- **Standards 6 项 / Spec 11 项**；已修：① 源码树 160 个 `*.pyc`（本会话 py_compile 误生成，已清零；教训：py_compile 显式写缓存不受 PYTHONDONTWRITEBYTECODE 控制）② Telegram telegram.me/ParseFailure ③ `use_universal` 排除 cover_url/images 媒体透传键（还原 Epic og 兜底走 media card 的旧行为）④ bridge 过时注释与 docstring ⑤ 测试重复导入/命名。
- **暂缓项（记录在 findings.md）**：media.py 键别名知识三处重复（_CONSUMED_KEYS/builder 长链/bridge 反向表）；builder 25+ 关键字参数双形态；music limitations 无条件写 favorite_count（既有行为保留）。

### 1.6 环境修复

- **venv 失效修复**：`ChatBot_Runtime/venv/pyvenv.cfg` 的 base python 指向不存在的 `C:\Program Files\Python312`（疑并行会话/环境变动）→ 改指 `C:\Users\LancyCelestia\AppData\Local\Programs\Python\Python312`（版本同为 3.12.10），venv 恢复可用。
- 源码树 `data/today_history_cache.json` 已删除（交接 §〇 指定，bot 未运行期执行）。

## 二、配置键速查（本周期新增）

| 键 | 默认 | 生效 | 说明 |
|---|---|---|---|
| `BOT_CONTENT_VIDEO_AUTO_SEND` | true | 热更 | 解析结果视频直发总开关（直链→下载→视频段） |

## 三、验证与运维（不变）

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -Command "& '.\scripts\dev.ps1' -Task test"   # 267 passed
powershell -NoProfile -ExecutionPolicy Bypass -Command "& '.\scripts\dev.ps1' -Task lint"
powershell -NoProfile -ExecutionPolicy Bypass -Command "& '.\scripts\dev.ps1' -Task runtime-layout"
```

## 四、剩余待办（§2.9 其余项，未做）

1. **9.3 分片幂等恢复**（part 级进度/UNKNOWN 确认协议/部分成功续发），需求见 handover-2026-08-29.md §9.3。
2. **10.4 深化残余**：Telegram fixture 覆盖（嵌套 DOM/纯媒体/置顶/多反应）与编辑/删除/用户名变化处理；小红书 `user_posted` 正文/时间/互动进 payload；YouTube Atom/shorts 类型；微博 card group/长文/置顶变体。
3. **10.5 音乐动态与榜单**（QQ/酷我/酷狗/Spotify 订阅、真实榜单 source）。
4. **10.6 其余**：target metadata 持久化 API（adapter 发现的 X rest ID 等显式持久化）；订阅旧契约清理复核。
5. **P4 Crawl Wiki `/bot wiki`**：按用户要求仍暂缓，不得扫描/导入 `D:\Coding\Crawl Wiki`。
6. **人工验收**：视频直发真机（QQ 发小红书视频链接看视频段是否送达）、Telegram 视频消息、XHS 真实笔记、X GraphQL 真实 Cookie、B 站杜比/Hi-Res、卡片渲染目检（Mica 规范）。

## 五、风险与约定（新增部分）

1. **venv 指向已被修复**；若再次报 "No Python at ..."，先查 `ChatBot_Runtime/venv/pyvenv.cfg` 的 home 是否被并行会话改动。
2. **不要用 `py_compile` 做语法检查**（会写 .pyc 进源码树）；用 pytest 或 `ast.parse`。
3. **`build_parsed_content` 是 parser 的唯一构造入口**；新增 parser 不得直接构造扁平字段，平台特有数据走 platform_extra。
4. **渲染投影在 bridge.flat_projection**：改卡片字段前先看投影与 `_KNOWN_STAT_KEYS`，避免卡片区块漂移。
5. 其余沿用交接 08-31：知识库 sync 删块语义、最小文件范围、密钥脱敏、不删活动数据、不 commit。
